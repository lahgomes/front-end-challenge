import styled from 'styled-components'

export const Title = styled.h1`
  color: var(--white);
  font-size: 2.4rem;
  font-weight: 700;

  @media (min-width: 768px) {
    font-size: 4.8rem;
    text-align: center;
    width: 78rem;
  }
`
